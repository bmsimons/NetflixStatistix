package presentation;

import java.awt.*;
// Interface for all classes that want to become an UserInterface (only have one)
public interface UI {
    public void createComponents(Container container);
}
